{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "No privileged frontend commands are exposed.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
