## Question

修复过程的实时步骤流在前端**长什么样**？用 /prototype 做一个廉价、粗糙但可交互的 HTML 原型（可纯静态、mock 事件数据），与用户一起确定：

1. 步骤列表的布局：步骤名 / 状态图标（进行中→成功/失败）/ 旧值 → 新值 的排版
2. 修复中：操作区如何收敛（按钮禁用态、流面板位置）
3. 失败时的呈现：错误信息内联显示 + 回滚步骤如何追加进流
4. 完成汇总：成功/失败计数 + 自动重测后的前后评分对比（如 62 → 8）如何展示
5. 检测过程的增量填充：固定顺序占位行、结果到达即填充的观感

已锁定约束（见地图 Notes 决策 4/7/8/12），本票只解决视觉与交互形态，不解决协议细节（那是「事件协议与回滚机制设计」）。
