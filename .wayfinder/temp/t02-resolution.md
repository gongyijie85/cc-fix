## ✅ 决议

**采用变体 B（步骤清单卡片）**，共享行为设定全部确认。

### 定稿形态（变体 B）

- 修复流以独立卡片呈现，标题随状态变化（"修复进行中…" → "修复完成" / "修复失败，已恢复原状"）
- 每步一行：序号圆圈（进行中脉冲/✓/✗/↩）+ 步骤名 + `旧值 → 新值`（等宽字体，新值加粗）+ 右侧状态徽章（成功/失败/已回滚）
- 失败时：错误信息以红色内联框附在该步下方（等宽字体，含原始错误文本）
- 回滚步骤以橙色 ↩ 圆圈追加进同一清单，标注"已回滚"徽章
- 底部汇总条：成功/失败计数徽章（+ 已回滚徽章）+ 右侧评分对比 `62 → 8`（复测完成后出现，绿色）

### 共享行为设定（三变体一致，已确认）

1. 修复进行中：顶部三个操作按钮全部禁用（配合服务端锁，见 #3）
2. 检测流：8 个信号固定顺序占位"检测中…"（脉冲动画），结果到达即填充值/风险徽章/分值
3. 失败流程：任一步失败 → 中止后续 → 自动回滚已改项 → 汇总标注"已回滚"

### 落选变体

- A（终端日志流）：信息密度高但非开发者可读性差，与现有卡片风格割裂
- C（分屏环境状态）："环境值闪光更新"点子好，可作为 B 的可选增强留作雾区参考，不进入本期

### 资产

- 原型（含全部三变体）：分支 `prototype/ticket-2-fix-flow-ui`（commit 890d2cc），本地副本 `.wayfinder/temp/prototype-fix-flow.html`
