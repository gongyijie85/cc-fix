# 地区状态分离与保护目标原子提交

cc-fix 将静态地区目录、日常偏好地区和保护生效地区分开管理，检测和临时运行只解析本次目标地区，不能隐式改写状态；非法地区明确报错，不再静默回落美国。保护强度与生效地区组成不可分割的保护目标，只有整次转换成功后才共同提交，失败则保留上次成功目标和原始日常备份。

## Consequences

保护期间允许直接换区或同时改变强度与地区；成功后生效地区成为新的偏好地区，退出保护仅清除生效地区。旧状态只有在地区合法或能由当前设置唯一匹配时自动迁移，否则标记需恢复；修复前后评分也只在目标地区一致时比较。
